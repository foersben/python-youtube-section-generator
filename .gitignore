# Python artifacts
__pycache__/
*.py[cod]
*.pyo
*.pyd
.Python
.python-version

# Virtual environments
.venv/
venv/
env/

# Tool caches & coverage
.mypy_cache/
.pytest_cache/
.coverage
htmlcov/
.cache/

# Packaging / build outputs
build/
dist/
*.egg-info/
pip-wheel-metadata/
site/

# GitHub operational/status files (generated)
/.github/status/

# IDE / editor
.idea/
.vscode/

# Local configuration & secrets (do NOT commit real keys)
.env
.secrets
# keep .secrets.baseline tracked if present

# Generated data & binary assets (root-only)
/sections.json
/transcript.json
/youtube_sections.txt
/webapp-linux
/bin/

# Local models: ignore large local model files in repo root, but keep source package models
/models/
!src/models/

# Local DBs, caches and translation artifacts (root-only)
/.chromadb/          # Chroma persistent DB and binary index files (kept local)
/chroma.sqlite3      # optional single-file DB
/.rag_cache/
/.translation_cache/

# If you intentionally place .chromadb deeper in the tree, consider adding
# a recursive ignore, but prefer a single known root location to avoid masking bugs:
# **/.chromadb/

# Local tooling helpers
setup-act.sh
.actrc

# Misc
.DS_Store
Thumbs.db
