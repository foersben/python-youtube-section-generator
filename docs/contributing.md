# Contributing

Thank you for contributing! Please follow the repository conventions:

- Source code under `src/`
- Tests under `tests/`
- Docs under `docs/`

Please run linting and unit tests locally before submitting PRs:

```bash
poetry run ruff check src tests
poetry run pytest -q
```

