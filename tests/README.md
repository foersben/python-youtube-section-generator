# Tests README (moved)

This README has been migrated into the project's MkDocs documentation.

See: Imported → Tests README (`docs/imported/tests_readme.md`).

Build docs locally with:

```bash
poetry run mkdocs serve
```

