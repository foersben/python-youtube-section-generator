# Scripts Cleanup Log (moved)

This file has been moved into the project's MkDocs documentation.

See: Imported → Scripts Cleanup (`docs/imported/scripts_cleanup.md`).

Build or serve docs locally with:

```bash
poetry run mkdocs serve
```

